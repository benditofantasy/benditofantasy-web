# Upstash Redis (poll votes). Connect via Vercel dashboard -> Storage -> Create
# Database -> Upstash -> Redis; Vercel injects these into the project automatically.
# For local dev: vercel link, then vercel env pull .env.local
KV_REST_API_URL=
KV_REST_API_TOKEN=
